<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<title>課題提出２</title>
</head>
<body>
	<h1>Editing Book</h1>
	
	<%= form_for(@book) do |f| %>

	<h4>Title</h4>
	<%= f.text_field :title %>

	<h4>Body</h4>
	<%= f.text_area :body %>

	<%= f.submit 'Update Book'%>
	
	<% end %>
	<p><%= link_to "Show", book_path(@book) %></p>
	<p><%= link_to "Back", books_path %></p>
</body>
</html>